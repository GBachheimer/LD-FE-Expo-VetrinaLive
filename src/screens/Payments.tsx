import { View, Text } from "src/components/Themed";
import React, { FC } from "react";

const Payments: FC = () => {
    return (
        <View>
            <Text>Payments</Text>
        </View>
    );
};

export default Payments