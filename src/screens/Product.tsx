import { View, Text } from "src/components/Themed";
import React, { FC } from "react";

const Product: FC = () => {
    return (
        <View>
            <Text>Product</Text>
        </View>
    );
};

export default Product