import { View, Text } from "src/components/Themed";
import React, { FC } from "react";

const Login: FC = () => {
    return (
        <View>
            <Text>Login</Text>
        </View>
    );
};

export default Login