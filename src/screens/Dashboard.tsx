import { View, Text } from "src/components/Themed";
import React, { FC } from "react";

const Dashboard: FC = () => {
    return (
        <View>
            <Text>Dashboard</Text>
        </View>
    );
};

export default Dashboard