import { View, Text } from "src/components/Themed";
import React, { FC } from "react";

const Admin: FC = () => {
    return (
        <View>
            <Text>Admin</Text>
        </View>
    );
};

export default Admin